# Audience Lab Design System

Self-contained reference implementation of the Audience Lab Tools design system — colors, typography, blueprint panel, cards, shell, and auth layouts — extracted from the production app. **`DESIGN_SYSTEM.md`** and **`tokens/tokens.json`** are the source of truth; the Vite app at `/login` and `/dashboard` is a pixel-faithful, logic-free demo of those tokens.
